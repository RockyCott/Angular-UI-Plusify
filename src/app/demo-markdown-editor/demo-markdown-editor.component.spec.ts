import { DemoMarkdownEditorComponent } from './demo-markdown-editor.component';

describe('DemoMarkdownEditorComponent', () => {
  let component: DemoMarkdownEditorComponent;

  beforeEach(() => {
    component = new DemoMarkdownEditorComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
